# Your Calendly scheduling page URL (e.g. https://calendly.com/your-username/30min)
REACT_APP_CALENDLY_URL=

# Optional: LLM chat endpoint (POST { message, history } → { reply })
# Use an Azure Function or API proxy — never put API keys in the React app
REACT_APP_AI_CHAT_ENDPOINT=
