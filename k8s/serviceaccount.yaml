apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload-identity-sa
  namespace: default
  annotations:
    azure.workload.identity/client-id: "WORKLOAD_IDENTITY_CLIENT_ID_PLACEHOLDER"
    azure.workload.identity/use: "true"

